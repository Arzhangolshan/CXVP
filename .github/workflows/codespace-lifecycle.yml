name: Codespace Lifecycle Manager

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        default: 'create'
        type: choice
        options:
          - create
          - stop
          - delete

jobs:
  manage-codespace:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup GitHub CLI
      run: |
        type -p curl >/dev/null || (sudo apt update && sudo apt install curl -y)
        curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
        sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
        echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
        sudo apt update
        sudo apt install gh -y

    - name: Authenticate with GitHub CLI
      run: echo "${{ secrets.CODESPACE_ACCESS_TOKEN }}" | gh auth login --with-token

    - name: Create Codespace
      if: github.event.inputs.action == 'create'
      run: |
        echo "Creating a new codespace..."
        CODESPACE_NAME=$(gh codespace create --repo ${{ github.repository }} --ref master --machine standardLinux32gb --display-name "g2ray-auto" --quiet)
        echo "CODESPACE_NAME=$CODESPACE_NAME" >> $GITHUB_ENV
        
        echo "Waiting for codespace to be ready..."
        sleep 30
        
        echo "Extracting config.json..."
        gh codespace cp -c $CODESPACE_NAME:/etc/config.json ./config_text.txt
        
        echo "Committing and pushing config_text.txt..."
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add config_text.txt
        git commit -m "Update g2ray config [skip ci]" || echo "No changes to commit"
        git push
        
        echo "Starting keep-alive script in background..."
        nohup python3 .github/scripts/keep_alive.py $CODESPACE_NAME &

    - name: Stop Codespace
      if: github.event.inputs.action == 'stop'
      run: |
        echo "Stopping codespace..."
        gh codespace stop -c ${{ env.CODESPACE_NAME }}

    - name: Delete Codespace
      if: github.event.inputs.action == 'delete'
      run: |
        echo "Deleting codespace..."
        gh codespace delete -c ${{ env.CODESPACE_NAME }} --force
